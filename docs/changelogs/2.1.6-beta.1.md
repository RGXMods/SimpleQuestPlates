# Simple Quest Plates! 2.1.6-beta.1

- Fixed options tab buttons not using the SQP brand green: the options
  panel now passes its brand theme through `UI:CreateOptionsPanel`, and
  RGX-Framework 2.7.8 resolves tab hover/active colors from the panel's
  theme instead of the shared default.

Requires RGX-Framework v2.7.8 or newer.
